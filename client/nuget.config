<?xml version="1.0" encoding="utf-8"?>
<configuration>
  <config>
    <!-- Меньше параллельных загрузок — реже обрывы "A task was canceled" при медленной сети. -->
    <add key="maxHttpRequestsPerSource" value="2" />
  </config>
</configuration>
